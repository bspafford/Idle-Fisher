#pragma once

#include "vector.h"

class stuff {
public:
	static inline float pixelSize = 3.f;
	static inline vector screenSize;
	static inline float animSpeed = 0.1f;
	static inline std::string name = "Idle Fisher";
	static inline std::string studio = "Fishy Games";
};